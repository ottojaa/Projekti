import { DuplicatesPipe } from './duplicates.pipe';

describe('DuplicatesPipe', () => {
  it('create an instance', () => {
    const pipe = new DuplicatesPipe();
    expect(pipe).toBeTruthy();
  });
});
